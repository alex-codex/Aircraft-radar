dash>=2.11.0
plotly>=5.15.0
pandas>=2.0.0
numpy>=1.24.0
sqlalchemy>=2.0.0
python-dotenv>=1.0.0
# Note: opensky-api doit être installé depuis sa source ou via pip s'il est dispo.
# Si le paquet opensky-api n'est pas sur PyPI public, l'utilisateur a déjà installé la bibliothèque localement.
opensky-api
